#ifndef __HASHTABLE_HPP
#define __HASHTABLE_HPP

#include "vector.hpp"
#include "hash.hpp"
namespace Ave {
	// We use seperate chaining for the hashtable
	// when the number of elements is greater than buckets, the hashtable do a rehashing
	template<typename T>
	class hashtable {

	};
}

#endif
